language: python
python:
  - 2.7
before_install:
  - sudo apt-get update
  - sudo apt-get install libgmp-dev binutils-dev
before_script:
  - cmake .
  - make
script:
  - ctest
notifications:
  email: false
